#define _CRT_SECURE_NO_WARRING
#include <stdio.h>

int main(void)
{
    int i=0;
    int sum=0;
    while (i<5)
    {
        int n;
        printf("값을입력하시오");
        scanf("%d", &n);
        sum+= n;
        i++;

    }

    printf("값은 %d입니다.\n", sum);
    return 0;
}
